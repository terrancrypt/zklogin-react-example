# Google OAuth Configuration
# Lấy từ Google Cloud Console → APIs & Services → Credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Redirect URI - URL sau khi đăng nhập thành công
VITE_REDIRECT_URI=http://localhost:5173

# Sui Network Configuration
# Devnet endpoint (dành cho development)
VITE_FULLNODE_URL=https://fullnode.devnet.sui.io

# Sui Prover API endpoint
VITE_PROVER_URL=https://prover-dev.mystenlabs.com/v1

# =====================================
# HƯỚNG DẪN SETUP:
# =====================================
# 
# 1. Copy file này thành .env:
#    cp env.example .env
#
# 2. Thay thế VITE_GOOGLE_CLIENT_ID bằng Client ID thật từ Google Cloud Console
#
# 3. Nếu chạy trên port khác (không phải 5173), thay đổi VITE_REDIRECT_URI
#
# 4. Các endpoint khác có thể giữ nguyên cho development
#
# =====================================
# LƯU Ý QUAN TRỌNG:
# =====================================
#
# - File .env này KHÔNG nên commit lên git
# - File .env đã được thêm vào .gitignore
# - Client ID có thể public nhưng nên hạn chế domain
# - Chỉ sử dụng trên devnet khi development
# - Không bao giờ để private keys trong .env
# 